"""
Елементи масиву A задаються так:
Обчислити z.
"""
import math
sum = 0
A = []
n=int(input("Введіть номер: "))
for i in range(1,n+1):
    a = math.pow(math.cos(1), (2*i-1)*math.cos(i))
    b = round(a/math.pow(1, i**2))
    A.append(b)
print(A)
for el in A:
    sum1 = 0
    p = el < 0
    h = p**2
    sum1 += p
print(sum1)
for el in A:
    if el > 0:
        c = el**2
        sum2 = sum + el
print(sum2)
if sum1 < sum2:
    print('z = -1')
else:
    print('z = 1')
